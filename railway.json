{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "if [ -f backend/requirements.txt ]; then python -m pip install -r backend/requirements.txt; else python -m pip install -r requirements.txt; fi"
  },
  "deploy": {
    "startCommand": "if [ -d backend ]; then cd backend; fi; python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "healthcheckPath": "/health"
  }
}
